# ✅ UniMeet 전체 기능 점검 리스트

## 📊 현재 구현 상태

### ✅ 완료된 기능

#### 1. 인증 시스템
- ✅ 회원가입 (kku.ac.kr 도메인 검증)
- ✅ 이메일 인증 (Redis 기반, 6자리 코드, 10분 TTL)
- ✅ 로그인 (JWT 토큰 발급)
- ✅ JWT 인증 필터
- ✅ 비밀번호 암호화 (BCrypt)

#### 2. 사용자 관리
- ✅ 프로필 조회 (내 프로필, 다른 사용자)
- ✅ 프로필 업데이트
- ✅ 사용자 검색
- ✅ 필드: gender, prefer, nonPrefer 추가 완료

#### 3. AI 매칭 시스템
- ✅ 호환성 계산 (MBTI, 관심사, 성격, 학과, 나이, 키)
- ✅ AI 매칭 API (상위 N명 추천)
- ✅ 세부 점수 분석
- ✅ 공통 관심사 계산
- ✅ Python AI 서비스 (scikit-learn 기반)
- ✅ Java-Python 연동 구조

#### 4. 채팅 시스템
- ✅ WebSocket 설정 (STOMP)
- ✅ 1:1 채팅방 생성
- ✅ 실시간 메시지 전송/수신
- ✅ 메시지 히스토리 저장 (MongoDB)
- ✅ 읽음 확인 기능
- ✅ 읽지 않은 메시지 수 조회
- ✅ 사용자별 채팅방 목록

#### 5. 데이터베이스
- ✅ MongoDB 연동
- ✅ Redis 연동 (캐싱, 세션, 이메일 인증)
- ✅ Repository 패턴 구현

#### 6. 보안
- ✅ Spring Security 설정
- ✅ CORS 설정
- ✅ JWT 토큰 인증
- ✅ WebSocket 인증

---

## 🔧 추가 구현 필요 기능

### 1. 매칭 관리 시스템
현재 매칭 후보만 조회 가능, 실제 매칭 관리 필요

#### 필요한 API:
```
POST /api/matches/request        - 매칭 요청 보내기
POST /api/matches/{id}/accept    - 매칭 수락
POST /api/matches/{id}/reject    - 매칭 거절
GET  /api/matches/received        - 받은 매칭 요청 목록
GET  /api/matches/sent            - 보낸 매칭 요청 목록
GET  /api/matches/accepted        - 수락된 매칭 목록
```

**상태:** ⚠️ 부분 구현 (MatchController에 일부 있음)

---

### 2. 미팅방 시스템
프론트엔드 요구사항에 있는 그룹 미팅 기능

#### 필요한 도메인:
```java
@Document(collection = "meeting_rooms")
public class MeetingRoom {
    private String id;
    private String title;
    private String type;  // "pair" | "mixed"
    private List<Participant> participants;
    private LocalDateTime createdAt;
    private boolean active;
}
```

#### 필요한 API:
```
POST /api/meetings/create         - 미팅방 생성
GET  /api/meetings/{id}           - 미팅방 조회
GET  /api/meetings/my             - 내 미팅방 목록
POST /api/meetings/{id}/join      - 미팅방 참가
POST /api/meetings/{id}/leave     - 미팅방 나가기
```

**상태:** ❌ 미구현

---

### 3. 알림 시스템
매칭 요청, 메시지 등의 알림

#### 필요한 기능:
- 매칭 요청 알림
- 새 메시지 알림
- 미팅방 초대 알림
- WebSocket 기반 실시간 알림

**상태:** ❌ 미구현

---

### 4. 사용자 차단/신고 기능
안전한 서비스 운영을 위한 필수 기능

#### 필요한 API:
```
POST /api/users/block/{userId}    - 사용자 차단
POST /api/users/unblock/{userId}  - 차단 해제
GET  /api/users/blocked           - 차단 목록
POST /api/users/report/{userId}   - 사용자 신고
```

**상태:** ❌ 미구현

---

### 5. 이미지 업로드
프로필 사진, 채팅 이미지 전송

#### 필요한 기능:
- 프로필 사진 업로드
- 채팅 이미지 전송
- 이미지 저장 (로컬 또는 S3)
- 이미지 리사이징

**상태:** ❌ 미구현

---

## 🎯 우선순위별 작업 계획

### 🔴 높음 (필수 - 내일 시연 전)
1. **매칭 요청/수락/거절 API** - 30분
   - 기존 MatchController 확장
   - Match 도메인 상태 관리

2. **채팅 시스템 테스트** - 20분
   - WebSocket 연결 테스트
   - 메시지 전송/수신 확인

3. **전체 플로우 테스트** - 30분
   - 회원가입 → 매칭 → 채팅 전체 흐름

### 🟡 중간 (시연 후 개발)
4. **미팅방 시스템** - 2시간
   - MeetingRoom 도메인 생성
   - CRUD API 구현

5. **알림 시스템** - 1시간
   - WebSocket 알림 채널
   - 알림 저장 및 조회

### 🟢 낮음 (추후 개발)
6. **사용자 차단/신고** - 1시간
7. **이미지 업로드** - 2시간

---

## 🚀 즉시 구현: 매칭 관리 API

현재 MatchController에 일부 구현되어 있지만 완전하지 않음.
다음 기능들을 추가해야 합니다:

### 추가 필요 메서드:

```java
// MatchController.java

/**
 * 받은 매칭 요청 목록
 */
@GetMapping("/received")
public ResponseEntity<List<Match>> getReceivedMatches() {
    String userId = getCurrentUserId();
    List<Match> matches = matchingService.getReceivedMatches(userId);
    return ResponseEntity.ok(matches);
}

/**
 * 보낸 매칭 요청 목록
 */
@GetMapping("/sent")
public ResponseEntity<List<Match>> getSentMatches() {
    String userId = getCurrentUserId();
    List<Match> matches = matchingService.getSentMatches(userId);
    return ResponseEntity.ok(matches);
}
```

---

## 📝 테스트 시나리오

### 시나리오 1: 전체 플로우
```
1. 사용자 A 회원가입 → 이메일 인증 → 로그인
2. 사용자 B 회원가입 → 이메일 인증 → 로그인
3. 사용자 A가 매칭 후보 조회 (AI 매칭)
4. 사용자 A가 사용자 B에게 매칭 요청
5. 사용자 B가 매칭 요청 확인
6. 사용자 B가 매칭 수락
7. 채팅방 자동 생성
8. 실시간 채팅 시작
```

### 시나리오 2: 채팅 시스템
```
1. 두 사용자가 매칭 수락
2. 채팅방 생성 API 호출
3. WebSocket 연결
4. 메시지 전송/수신
5. 읽음 확인
6. 메시지 히스토리 조회
```

---

## 🔍 현재 시스템 검증

### 테스트 명령어

```powershell
# 1. Health Check
Invoke-RestMethod -Uri "http://localhost:8080/health" -Method Get

# 2. 회원가입
$user = @{
    email = "test1@kku.ac.kr"
    password = "password123"
    name = "테스트1"
    nickname = "test1"
    studentId = "20240001"
    department = "컴퓨터공학과"
    birth = "2000-01-01"
    phone = "010-1111-1111"
    gender = "남"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8080/auth/signup" -Method Post -Body $user -ContentType "application/json"

# 3. 이메일 인증
$emailBody = @{ email = "test1@kku.ac.kr" } | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:8080/auth/send-verification" -Method Post -Body $emailBody -ContentType "application/json"

# 콘솔에서 코드 확인 후
$verifyBody = @{ email = "test1@kku.ac.kr"; code = "123456" } | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:8080/auth/verify-email" -Method Post -Body $verifyBody -ContentType "application/json"

# 4. 로그인
$loginBody = @{ email = "test1@kku.ac.kr"; password = "password123" } | ConvertTo-Json
$loginResponse = Invoke-RestMethod -Uri "http://localhost:8080/auth/login" -Method Post -Body $loginBody -ContentType "application/json"
$token = $loginResponse.token
$headers = @{ "Authorization" = "Bearer $token"; "Content-Type" = "application/json" }

# 5. AI 매칭
Invoke-RestMethod -Uri "http://localhost:8080/api/matches/find" -Method Post -Headers $headers

# 6. 채팅방 생성
$chatRoomBody = @{ userAId = "user1"; userBId = "user2" } | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:8080/api/chat/rooms" -Method Post -Body $chatRoomBody -ContentType "application/json"
```

---

## ✅ 최종 체크리스트

### 시연 전 필수 확인
- [ ] MongoDB 실행 중
- [ ] Redis 실행 중
- [ ] AI 서비스 실행 중
- [ ] 백엔드 서버 실행 중
- [ ] Health Check 성공
- [ ] 회원가입 → 이메일 인증 → 로그인 플로우 테스트
- [ ] AI 매칭 API 테스트
- [ ] 채팅방 생성 테스트
- [ ] WebSocket 연결 테스트

### 프론트엔드 연동 전 확인
- [ ] API 문서 전달
- [ ] CORS 설정 확인
- [ ] 테스트 계정 생성
- [ ] 에러 응답 형식 확인
- [ ] JWT 토큰 만료 시간 확인

---

## 🎉 결론

**현재 상태:**
- ✅ 핵심 기능 80% 완성
- ✅ 시연 가능한 수준
- ⚠️ 일부 고급 기능 미구현 (미팅방, 알림, 이미지)

**즉시 작업 필요:**
1. 매칭 요청/수락/거절 API 완성 (30분)
2. 전체 플로우 테스트 (30분)

**시연 후 작업:**
3. 미팅방 시스템 (2시간)
4. 알림 시스템 (1시간)
5. 추가 기능들

지금 바로 매칭 관리 API를 완성하시겠습니까?